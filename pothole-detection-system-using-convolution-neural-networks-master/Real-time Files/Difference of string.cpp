class Solution {
public:
    char findTheDifference(string s, string t) {
        string a;
        string b;
        a=s;
        b=t;
        sort(a.begin(),a.end());
        sort(b.begin(),b.end());
        
    }
};